{
  "name": "approvelens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Audit whether an AI tool approval view faithfully represents its raw payload.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/analyzer.js && node --check src/app.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
